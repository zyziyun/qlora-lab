# Only needed when you compare against a frontier API baseline (notebook 01/05).
# The data, training, and adapter-vs-base comparison need no key.
OPENAI_API_KEY=sk-...

# Optional: point the client at a local vLLM server instead of OpenAI.
# OPENAI_BASE_URL=http://localhost:8000/v1
